<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:SportsSupporter"
       Title="SportsSupporter"
       x:Class="SportsSupporter.AppShell">

    <TabBar>
        <!-- Karta: Monitorowanie ćwiczeń -->
        <ShellContent
            Title="Ćwiczenia"
            ContentTemplate="{DataTemplate local:MainPage}" />

        <!-- Karta: Plany treningowe -->
        <ShellContent
            Title="Plany"
            ContentTemplate="{DataTemplate local:Plany}" />
    </TabBar>

</Shell>
